# quickCV
## 功能概述
软件名为quickCV，选用超轻量级的开发框架，制作一个开箱即用的文字模板插入管理工具，可以编辑预设内容，然后在输入时快捷插入或替换文本，支持通过webdav同步数据。

## 要求
- 支持windows和macos（优先开发windows版本，但你选用的框架要兼容macos，以便之后适配macos版）
- 简体中文界面，注意模板和搜索、内容都要支持中文编码
- 简洁、紧凑、开箱即用

## 界面设计

### 快捷键触发页
- 一个带文件夹级的快捷输入选项浮窗，通过预设快捷键触发，上面有搜索功能，可搜索文件夹和模板名称，支持键盘上下选择，左右切换文件夹和模板栏，回车插入，esc取消，也支持鼠标操作
- 浮窗出现位置为当前输入光标处
- 自动获取焦点，在选中插入和失去焦点后自动关闭
- 简洁紧凑的浮窗，丝滑的效果，取消系统自带的最小化、关闭等边框

### 设置页
- 包含设置项：
  - 快捷键设置（直接录制快捷键）
  - 是否开机启动
  - webdav设置：地址、用户名、密码（带测试连通性功能）
- 显示当前版本号和手动检查更新
- 模板管理
  - 文件夹管理
  - 文件夹下的模板管理，模板就是一段字符串，用于快捷插入
  - 每个模板可以设置key，key非必填，但不可重复，key的作用在下文会提到
  - 注意模板部分编辑的便利性，仔细思考，发挥想象力设计舒适的使用体验
  - 有两个按钮，从云端拉取数据和推送数据到云端，记录数据版本号(时间戳)，当同步时云端数据比本地新，提示用户先拉取

## 全局监听及自动替换功能
- 实时记录用户键盘录入(但需要注意性能，不能影响用户正常操作)，当用户录入^{key}^时，自动替换为模板内容，并自动删除^{key}^
- 该功能需要全局监听，无论用户在什么软件上编辑文字，都能实现自动替换

## 系统相关功能
- 通过github actions实现自动打包，当提交tag时触发，并发布release
- 软件启动时自动检查更新，若有更新询问用户是否更新，并实现自动从github更新的功能
- 系统托盘区图标，使用/static/logo.png，右键菜单有设置和退出


- 支持录入符号'^'触发弹出层，'^{key}'中key可从模板中搜索（默认选中第一个），键盘上下选择，回车插入，esc取消
- 支持自定义Webdav服务器实现多端同步，还要处理多端同时使用的文件合并问题
- 支持设置开机自启动
- 通过github actions线上打包、自动更新
