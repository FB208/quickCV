{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default-capability",
  "description": "quickCV 默认能力",
  "windows": ["main", "overlay"],
  "permissions": [
    "core:default",
    "dialog:default",
    "opener:default",
    "autostart:default",
    "global-shortcut:default"
  ]
}
